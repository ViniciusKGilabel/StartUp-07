import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    default: {
        width: '100%',
        padding: '20 px',
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 3,
    },
})